[tool.ruff]
target-version = "py312"
line-length = 120
extend-exclude = [
  "frontend",
  "migrations",
]

[tool.ruff.lint]
select = [
  "E9",
  "F",
  "I",
]

[tool.ruff.lint.isort]
known-first-party = [
  "api",
  "pipeline",
  "news_scraper_project",
]
